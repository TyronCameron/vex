```lua
{
	taskfolder = '.',
	default = {
		taskformat = 'obsidian',
		view = 'table',
		dataformat = CSV,
		option = 'prev', 
		tasktype = 'task'
	}, 
	plugins = {
		vexations = true,
		inline = true,
		folderize = true
	}
}
```