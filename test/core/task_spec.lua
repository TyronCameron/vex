-- Test file for core/task.lua using busted
local busted = require 'busted'

describe("Task", function()
  it("should create a new task manager", function()
    -- This test is a placeholder because the task module requires plugin system
    -- which is initialized at module load time and causes issues in test environment
    assert.truthy(true)
  end)
end)